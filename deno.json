{
  "nodeModulesDir": "auto",
  "tasks": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "check": "vue-tsc --noEmit",
    "preview": "vite preview",
    "tauri": "tauri",
    "fetch-llama-server": "deno run --allow-env --allow-net --allow-write --allow-read --allow-run scripts/fetch-llama-server.ts"
  }
}
