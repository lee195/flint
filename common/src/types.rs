//! IPC-shared types placeholder. Grows in Phase 0 (probe result, cookbook
//! recommendation) and Phase 1 (chat events). Frontend mirror lives at
//! `src/lib/types.ts`; IPC structs here are serde-renamed to camelCase.
