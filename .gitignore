# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Rust workspace build output (lives at workspace root, not per-crate)
/target/

# Deno lockfile for npm deps
deno.lock

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Bundled sidecars (build artifacts, produced by the bundle scripts — Phase 2)
src-tauri/binaries/
